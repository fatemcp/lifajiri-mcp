# FateMCP LifaJiri Skill

## 简介

FateMCP LifaJiri（理发吉日）：MCP 服务，综合藏汉历法推算理发、剃头、洗头吉日与吉时。

## 触发场景

当用户询问以下内容时调用：
- 理发吉日 / 适合理发的日子
- 剃头吉时 / 洗头吉日
- 藏历理发 / 文殊菩萨剃头吉日

## 调用方式

配置 MCP 客户端接入 `https://mcp.fatemcp.com/lifajiri/mcp` 后，调用工具：

### 工具一：`get_haircut_auspicious_days`

查询指定日期范围内的理发吉日**列表**。

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `start_date` | string | 否 | 开始日期（YYYY-MM-DD），默认今天 |
| `days` | integer | 否 | 查询天数（1-365），默认 30 |

**返回：** 吉日总数 + 每个吉日的公历/农历/藏历信息、文殊吉凶、藏历理发吉凶、吉时列表。

### 工具二：`get_nearest_haircut_day`

查询距离指定日期（默认今天）最近的**下一个**理发吉日，返回该日完整信息与吉时。

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `from_date` | string | 否 | 从该日期起向后找（YYYY-MM-DD），默认今天 |
| `lookahead` | integer | 否 | 最多向后找多少天（1-60），默认 30 |

**返回：** 最近吉日的日期、星期、农历/藏历信息、文殊吉凶、藏历理发吉凶、完整吉时列表。

## 使用建议

- 用户问"某月/某段时间有哪些吉日" → 用工具一（列表）
- 用户问"最近/接下来哪天适合理发" → 用工具二（最近单日）
- 展示吉日时附带吉时
- 无吉日则建议用户扩大范围

## 隐私

所有计算在服务器本地完成，不存储、不记录请求信息。
